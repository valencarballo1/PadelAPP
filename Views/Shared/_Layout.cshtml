<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Padel APP</title>

    <link href="~/Content/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
          rel="stylesheet">

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/modernizr")
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="styxlesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/css/bootstrap-datepicker.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/locales/bootstrap-datepicker.es.min.js"></script>
    <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.5.1/css/all.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment-timezone/0.5.33/moment-timezone-with-data-10-year-range.min.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/2.0.0/css/dataTables.dataTables.css" />
    <script src="https://cdn.datatables.net/2.0.0/js/dataTables.js"></script>
    <link rel="stylesheet" type="text/css" href="~/Content/slick-1.8.1/slick/slick.css" />
    <link rel="stylesheet" type="text/css" href="~/Content/slick-1.8.1/slick/slick-theme.css" />
    <link href="~/Content/fontawesome-free/css/all.css" rel="stylesheet" />
    <link href="~/Content/CSSADMIN/sb-admin-2.min.css" rel="stylesheet">
    <link href="~/Content/Site.css" rel="stylesheet" />



    <style>
        .hidden {
            display: none
        }
    </style>
</head>

<body id="page-top">

    <div id="wrapper">

        <ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">
            <div class="">
                <a class="sidebar-brand row d-flex align-items-center justify-content-center" href="@Url.Action("Index", "Home")">
                    <div class="sidebar-brand-icon rotate-n-15">
                        <i class="fas fa-baseball-ball"></i>
                    </div>
                </a>
            </div>

            <hr class="sidebar-divider my-0">

            <li class="nav-item active">
                <a class="nav-link" href="@Url.Action("Index", "Administracion")">
                    <i class="fas fa-fw fa-tachometer-alt"></i>
                    <span>Administracion</span>
                </a>
            </li>

            <hr class="sidebar-divider">

            <hr class="sidebar-divider">

            <div class="sidebar-heading">
                Addons
            </div>

            <li class="nav-item">
                <a class="nav-link collapsed" href="#collapsePages" data-bs-toggle="collapse" data-target="#collapsePages"
                   aria-expanded="true" aria-controls="collapsePages">
                    <i class="fas fa-fw fa-folder"></i>
                    <span>Paginas</span>
                </a>
                <div class="collapse" id="collapsePages" aria-labelledby="headingPages" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Usuarios:</h6>
                        <div class="hidden" id="miPerfilPaginas">
                            <a class="collapse-item" href="@Url.Action("Perfil", "Usuario")">Mi perfil</a>
                        </div>
                        <a class="collapse-item" href="@Url.Action("Disponibles", "Partido")">Ver partidos creados</a>
                        <a class="collapse-item" href="@Url.Action("Ranking", "Home")">Ver ranking</a>
                    </div>
                </div>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Crear", "Partido")">
                    <i class="fas fa-fw fa-chart-area"></i>
                    <span>Crear Partido</span>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Reserva", "Home")">
                    <i class="fa-solid fa-circle-plus"></i>
                    <span>Reservar</span>
                </a>
            </li>

            <hr class="sidebar-divider d-none d-md-block">

            <div class="text-center d-none d-md-inline">
                <button class="rounded-circle border-0" id="sidebarToggle"></button>
            </div>

        </ul>

        <div id="content-wrapper" class="d-flex flex-column">

            <div id="content">

                <nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">

                    <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
                        <i class="fa fa-bars"></i>
                    </button>


                    <ul class="navbar-nav ml-auto">


                        <li class="nav-item dropdown no-arrow mx-1 hidden" id="notificacion">
                            <a class="nav-link dropdown-toggle" href="#" id="alertsDropdown" role="button"
                               data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="fas fa-bell fa-fw" id="alertDropdownIcon"></i>
                                <span class="badge badge-danger badge-counter" id="contadorNotificaciones"></span>
                            </a>
                        </li>

                        <div class="topbar-divider d-none d-sm-block"></div>

                        <div class="d-flex align-items-center">
                            <a type="button" class="btn btn-primary btn-sm" id="btnInicioSesion" href="@Url.Action("InicioSesion", "Usuario")">Iniciar Sesion</a>
                        </div>
                        <li class="nav-item dropdown no-arrow hidden" id="liPerfil">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button"
                               data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <span class="mr-2 d-none d-lg-inline text-gray-600 small" id="usuarioNombre"></span>
                                <img class="img-profile rounded-circle" id="usuarioImg"
                                     src="img/undraw_profile.svg">
                            </a>
                            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
                                 aria-labelledby="userDropdown">
                                <a class="dropdown-item" href="#">
                                    <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Profile
                                </a>

                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">
                                    <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Logout
                                </a>
                            </div>
                        </li>

                    </ul>

                </nav>

                <div class="container">
                    @RenderBody()
                </div>
            </div>

        </div>

    </div>

    <a class="scroll-to-top rounded" href="#page-top">
        <i class="fas fa-angle-up"></i>
    </a>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
    <script type="text/javascript" src="~/Content/slick-1.8.1/slick/slick.min.js"></script>
    <script src="~/Content/js/sb-admin-2.min.js"></script>

    <script>
        let usuarioId;
        let esAdmin = false;
        usuarioId = getCookieValue("UsuarioSesion")

        function enEspera(modo = "on", titulo = "Procesando...", texto = "Aguarde un instante") {
            if (modo == "on") {
                Swal.fire({
                    title: titulo,
                    html: texto,
                    allowOutsideClick: false,
                    showCancelButton: false,
                    showConfirmButton: false,
                    showDenyButton: false
                });
                Swal.showLoading();
            } else {
                Swal.close();
            }
        }

        function modalAwait(modo = "show") {

            if (modo == "show") {

                $("#espera_contenedor").css("top", window.scrollY)

                $("#espera_contenedor").css("visibility", "visible")

            } else {

                $("#espera_contenedor").css("visibility", "hidden")

            }

        }

        $(document).ready(function () {
            checkUserSession();
        });

        function checkUserSession() {
            enEspera();
            $.ajax({
                url: "@Url.Action("ValidarSesion", "Usuario")",
                type: 'GET',
                success: function (response) {
                    enEspera("chau");
                    if (response.estaActivo) {
                        $('#usuarioNombre').text(response.usuarioNombre);
                        $('#usuarioImg').attr('src', response.usuarioImg);
                        $('#usuarioImg').removeClass('hidden');
                        $('#liPerfil').removeClass('hidden');
                        $('#btnInicioSesion').addClass('hidden');
                        $('#notificacion').removeClass('hidden');
                        $('#miPerfilPaginas').removeClass('hidden');
                        llenarNotificaciones();

                    } else {
                        enEspera("chau");
                        $('#usuarioImg').addClass('hidden');
                    }
                },
                error: function () {
                    enEspera("chau");
                    console.error('Error al verificar la sesión del usuario.');
                }
            });
        }

        function getCookieValue(cookieName) {
            var name = cookieName + "=";
            var decodedCookie = decodeURIComponent(document.cookie);
            var cookieArray = decodedCookie.split(';');

            for (var i = 0; i < cookieArray.length; i++) {
                var cookie = cookieArray[i].trim();
                if (cookie.indexOf(name) === 0) {
                    var cookieValue = cookie.substring(name.length, cookie.length);
                    var keyValueArray = cookieValue.split('=');

                    if (keyValueArray.length > 1) {
                        return keyValueArray[1];
                    }
                }
            }
            return null;
        }

        function obtenerUsuarioId() {
            console.log("retorno llamada " + usuarioId);
            return usuarioId;
        }

        $('#alertDropdownIcon').click(function (event) {
            event.preventDefault();
            $('#notificacion .dropdown-menu').toggleClass('show');

            $.ajax({
                url: "@Url.Action("LeerNotifiaciones", "Notificacion")",
                type: 'GET',
                success: function (data) {
                    if (data == true) {
                        console.log("Notificaciones leidas");
                        $('#contadorNotificaciones').text('');

                    } else {
                        console.log("No existen notificaciones");
                    }
                },
                errror: function () {
                    console.log("Error server");
                }
            });
        });

        $(document).click(function (event) {
            var target = $(event.target);
            if (!target.closest('#notificacion').length && $('.dropdown-menu').hasClass('show')) {
                $('.dropdown-menu').removeClass('show');
            }
        });

        function llenarNotificaciones() {
            $.ajax({
                url: "@Url.Action("Notificaciones", "Notificacion")",
                type: 'GET',
                success: function (data) {
                    if (data.length > 0) {
                        $('#contadorNotificaciones').text(data.length);
                        const notificacionesDiv = $('<div class="dropdown-menu dropdown-menu-right p-3" aria-labelledby="alertsDropdown"></div>');
                        let detalleNotificacion;
                        data.forEach(function (notificacion) {
                            let icono;
                            if (notificacion.TipoNotificacion === 1) {
                                const verPartido = "@Url.Action("Ver", "Partido")?idPartido=" + data.idCanchaReservada;
                                icono = '<i class="fa-solid fa-table-tennis-paddle-ball fa-lg" style="color: #63E6BE;"><circle cx="8" cy="8" r="8"/></i>';
                                detalleNotificacion = $('<a href="verPartido" class="dropdown-item d-flex align-items-center" href="#"><div class="mr-3">' + icono + '</div><div>' + notificacion.Detalle + '</div></a>');
                            } else if (notificacion.TipoNotificacion === 2) {
                                icono = '<i class="fas fa-exclamation fa-lg" style="color: #74C0FC;"></i>';
                                detalleNotificacion = $('<div class="dropdown-item d-flex align-items-center" href="#"><div class="mr-3">' + icono + '</div><div>' + notificacion.Detalle + '</div></div>');
                            }
                            notificacionesDiv.append(detalleNotificacion);
                        });
                        $('#notificacion').append(notificacionesDiv);


                    } else {

                    }
                },
                error: function () {
                    console.error('Error al verificar la sesión del usuario.');
                }

            });
        }


    </script>
</body>
</html>
