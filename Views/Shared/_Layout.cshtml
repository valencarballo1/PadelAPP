<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Padel, Club de padel, Crea partidos, Juga al padel">
    <meta name="author" content="Valentin Carballo">

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/modernizr")
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.datatables.net/2.0.0/js/dataTables.js"></script>
    <link href="https://preview.tabler.io/dist/css/tabler.min.css?1695847769" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/locales/bootstrap-datepicker.es.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment-timezone/0.5.33/moment-timezone-with-data-10-year-range.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/css/bootstrap-datepicker.min.css">
    <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.5.1/css/all.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/2.0.0/css/dataTables.dataTables.css" />
    <link rel="stylesheet" type="text/css" href="~/Content/slick-1.8.1/slick/slick.css" />
    <link rel="stylesheet" type="text/css" href="~/Content/slick-1.8.1/slick/slick-theme.css" />
    <link href="~/Content/Site.css" rel="stylesheet" />
    <link rel="shortcut icon" href="~/Content/CssHome/994003_court_racquet_sport_squash_tennis_icon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="~/Content/CssHome/994003_court_racquet_sport_squash_tennis_icon.ico">
    <title>Padel APP</title>
</head>

<body class="bg-light" id="page-top">
    <header class="navbar navbar-expand-md navbar-dark bg-primary">
        <div class="container-xl">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-menu" aria-controls="navbar-menu" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                <img src="~/imgPerfiles/AgregarAPartido.png" alt="Logo" class="navbar-brand-image logo">
            </a>
            <div class="navbar-nav flex-row">
                <div class="nav-item d-none d-md-flex me-3">
                    <div class="btn-list">
                        <a class="btn btn-outline-light" href="@Url.Action("Crear", "Partido")">
                            <i class="fa-solid fa-table-tennis-paddle-ball me-2"></i>
                            <span>Crear Partido</span>
                        </a>
                        @*<a class="btn btn-outline-light" href="@Url.Action("Reserva", "Home")">
                            <i class="fa-solid fa-circle-plus me-2"></i>
                            <span>Reservar</span>
                        </a>*@
                        
                        <button class="btn btn-outline-light hidden" id="alertDropdownIcon" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-bell fa-fw"></i>
                            <span class="badge bg-danger" id="contadorNotificaciones"></span>
                        </button>
                    </div>
                </div>
            </div>
            <div class="d-flex align-items-center">
                <a href="@Url.Action("InicioSesion", "Usuario")" class="btn btn-secondary me-2" id="btnInicioSesion">Iniciar Sesión</a>
                <a href="@Url.Action("Perfil", "Usuario")" class="nav-link d-flex align-items-center text-light" id="menuUserMiniatura">
                    <span class="avatar avatar-sm me-2" style="background-image: url('../../imgperfiles/AgregarAPartido.png');" id="usuarioImg"></span>
                    <div class="d-none d-xl-block" id="usuarioNombre"></div>
                </a>
            </div>
        </div>
    </header>

    <nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm">
        <div class="container-xl">
            <div class="collapse navbar-collapse" id="navbar-menu">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link hidden" id="panelAdmin" href="@Url.Action("Index", "Administracion")">
                            <i class="fas fa-fw fa-tachometer-alt me-2"></i>
                            <span>Administración</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Ranking", "Home")">
                            <i class="fa-solid fa-ranking-star me-2"></i>
                            <span>Ver ranking</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Crear", "Partido")">
                            <i class="fa-solid fa-table-tennis-paddle-ball me-2"></i>
                            <span>Crear Partido</span>
                        </a>
                    </li>
                    @*<li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Reserva", "Home")">
                            <i class="fa-solid fa-circle-plus me-2"></i>
                            <span>Reservar</span>
                        </a>
                    </li>*@
                    <li class="nav-item">
                        <a class="nav-link hidden" id="reservasAdmin" href="@Url.Action("Reservas", "Administracion")">
                            <i class="fas fa-fw fa-table me-2"></i>
                            <span>Panel de reservas</span>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4">
        @RenderBody()
    </main>

    <footer class="bg-dark text-light py-4">
        <div class="container">
            <div class="row justify-content-center align-items-center seccion-logos mb-4">
                <div class="col-6 col-md-3 text-center">
                    <img src="https://cdn.shopify.com/s/files/1/2232/3715/files/nox_negativo_red_pantone_100x@2x.jpg?v=1613541634" alt="Logo 1" class="img-fluid rounded">
                </div>
                <div class="col-6 col-md-3 text-center">
                    <img src="https://seeklogo.com/images/P/Penn-logo-7483438826-seeklogo.com.gif" alt="Logo 2" class="img-fluid rounded">
                </div>
                <div class="col-6 col-md-3 text-center">
                    <img src="https://logospng.org/download/gatorade/gatorade-4096.png" alt="Logo 3" class="img-fluid rounded">
                </div>
                <div class="col-6 col-md-3 text-center">
                    <img src="https://loodibee.com/wp-content/uploads/Adidas-Logo.png" alt="Logo 4" class="img-fluid rounded">
                </div>
            </div>
            <div class="row">
                <div class="col-12 text-center">
                    <p>&copy; @DateTime.Now.Year - Si tienes alguna idea para mejorar el proyecto, contáctame: valentincarballo99@gmail.com</p>
                </div>
            </div>
        </div>
    </footer>

    <a class="scroll-to-top rounded" href="#page-top">
        <i class="fas fa-angle-up"></i>
    </a>


    @Scripts.Render("~/bundles/bootstrap")
    <script src="~/Scripts/jquery.fancybox.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script type="text/javascript" src="~/Content/slick-1.8.1/slick/slick.min.js"></script>

    <script>
        let usuarioId;
        let esAdmin = false;
        usuarioId = getCookieValue("UsuarioSesion")

        function enEspera(modo = "on", titulo = "Procesando...", texto = "Aguarde un instante") {
            if (modo == "on") {
                Swal.fire({
                    title: titulo,
                    html: texto,
                    allowOutsideClick: false,
                    showCancelButton: false,
                    showConfirmButton: false,
                    showDenyButton: false
                });
                Swal.showLoading();
            } else {
                Swal.close();
            }
        }

        function modalAwait(modo = "show") {

            if (modo == "show") {

                $("#espera_contenedor").css("top", window.scrollY)

                $("#espera_contenedor").css("visibility", "visible")

            } else {

                $("#espera_contenedor").css("visibility", "hidden")

            }

        }

        $(document).ready(function () {
            checkUserSession();
        });

        function checkUserSession() {
            enEspera();
            $.ajax({
                url: "@Url.Action("ValidarSesion", "Usuario")",
                type: 'GET',
                success: function (response) {
                    enEspera("chau");
                    if (response.estaActivo) {
                        $('#usuarioNombre').text(response.usuarioNombre);
                        $('#usuarioImg').css('background-image', 'url(' + response.usuarioImg + ')');
                        $('#usuarioImg').removeClass('hidden');
                        $('#liPerfil').removeClass('hidden');
                        $('#btnInicioSesion').addClass('hidden');
                        $('#alertDropdownIcon').removeClass('hidden');
                        $('#miPerfilPaginas').removeClass('hidden');
                        $('#menuUserMiniatura').removeClass('hidden');
                        llenarNotificaciones();
                        if (response.esAdmin) {
                            $('#reservasAdmin').removeClass('hidden');
                            $('#panelAdmin').removeClass('hidden');
                        }

                    } else {
                        enEspera("chau");
                        $('#usuarioImg').addClass('hidden');
                    }
                },
                error: function () {
                    enEspera("chau");
                    console.error('Error al verificar la sesión del usuario.');
                }
            });
        }

        function getCookieValue(cookieName) {
            var name = cookieName + "=";
            var decodedCookie = decodeURIComponent(document.cookie);
            var cookieArray = decodedCookie.split(';');

            for (var i = 0; i < cookieArray.length; i++) {
                var cookie = cookieArray[i].trim();
                if (cookie.indexOf(name) === 0) {
                    var cookieValue = cookie.substring(name.length, cookie.length);
                    var keyValueArray = cookieValue.split('=');

                    if (keyValueArray.length > 1) {
                        return keyValueArray[1];
                    }
                }
            }
            return null;
        }

        function obtenerUsuarioId() {
            console.log("retorno llamada " + usuarioId);
            return usuarioId;
        }

        $('#alertDropdownIcon').click(function (event) {
            event.preventDefault();
            $('#notificacion .dropdown-menu').toggleClass('show');

            $.ajax({
                url: "@Url.Action("LeerNotifiaciones", "Notificacion")",
                type: 'GET',
                success: function (data) {
                    if (data == true) {
                        console.log("Notificaciones leidas");
                        $('#contadorNotificaciones').text('');

                    } else {
                        console.log("No existen notificaciones");
                    }
                },
                errror: function () {
                    console.log("Error server");
                }
            });
        });

        $(document).click(function (event) {
            var target = $(event.target);
            if (!target.closest('#notificacion').length && $('.dropdown-menu').hasClass('show')) {
                $('.dropdown-menu').removeClass('show');
            }
        });

       function llenarNotificaciones() {
     $.ajax({
         url: "@Url.Action("Notificaciones", "Notificacion")",
         type: 'GET',
         success: function (data) {
             console.log(data);
             if (data.length > 0) {
                 $('#contadorNotificaciones').text(data.length);
                 const notificacionesDiv = $('<div class="dropdown-menu dropdown-menu-right p-3" aria-labelledby="alertsDropdown"></div>');
                 let detalleNotificacion;
                 data.forEach(function (notificacion) {
                     let icono;
                     if (notificacion.TipoNotificacion === 1) {
                         let idPartido = notificacion.IdCanchaReservada;
                         let urlVerPartido = "@Url.Action("Ver", "Partido")?idPartido=" + idPartido;
                         console.log(urlVerPartido);
                         icono = '<i class="fa-solid fa-table-tennis-paddle-ball fa-lg" style="color: #63E6BE;"><circle cx="8" cy="8" r="8"/></i>';
                         detalleNotificacion = $('<a href="' + urlVerPartido + '" class="dropdown-item d-flex align-items-center" href="#"><div class="mr-3">' + icono + '</div><div>' + notificacion.Detalle + '</div></a>');
                     } else if (notificacion.TipoNotificacion === 2) {
                         icono = '<i class="fas fa-exclamation fa-lg" style="color: #74C0FC;"></i>';
                         detalleNotificacion = $('<div class="dropdown-item d-flex align-items-center" href="#"><div class="mr-3">' + icono + '</div><div>' + notificacion.Detalle + '</div></div>');
                     }
                     notificacionesDiv.append(detalleNotificacion);
                 });
                 $('#notificacion').append(notificacionesDiv);


             } else {

             }
         },
         error: function () {
             console.error('Error al verificar la sesión del usuario.');
         }

     });
}


    </script>
</body>
</html>
